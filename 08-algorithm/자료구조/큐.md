---
tags: [algorithm, queue]
status: completed
created: 2025-12-09
---
# 큐 (Queue)
데이터를 저장하는 **선형 자료구조**로, 선입선출(First In First Out) 방식을 따른다.

## 핵심 개념

핵심 연산
- enqueue() : 데이터를 제일 마지막에 추가
- dequeue() : 가장 먼저 들어온 데이터를 제거

<div style="background:#1e1e2e; border-radius:8px; padding:16px; font-family:monospace; color:#cdd6f4; margin:12px 0;">
<div style="color:#89b4fa; font-weight:bold; margin-bottom:8px;">Queue — FIFO</div>
<div style="display:flex; align-items:center; gap:4px;">
<span style="color:#a6e3a1;">dequeue ←</span>
<div style="display:flex; border:2px solid #45475a; border-radius:4px; padding:4px; gap:2px;">
<div style="background:#45475a; padding:4px 12px; border-radius:3px; color:#f38ba8; border:1px solid #f38ba8;">1</div>
<div style="background:#45475a; padding:4px 12px; border-radius:3px; color:#a6e3a1;">2</div>
<div style="background:#45475a; padding:4px 12px; border-radius:3px; color:#a6e3a1;">3</div>
</div>
<span style="color:#f9e2af;">← enqueue</span>
</div>
</div>

## Priority Queue

일반 Queue는 FIFO 순서를 따르지만, **Priority Queue**는 **우선순위가 높은 원소**가 먼저 나온다.

내부적으로 **Heap(완전 이진 트리)** 기반으로 구현된다.

| | Queue | Priority Queue |
|--|-------|----------------|
| **원리** | FIFO (먼저 들어온 순) | 우선순위 높은 순 |
| **내부 구조** | Array 또는 LinkedList | **Heap (완전 이진 트리)** |
| **삽입** | O(1) | O(log n) |
| **삭제(poll)** | O(1) | O(log n) |
| **최솟/최댓값 조회** | O(n) | O(1) |

정렬된 배열로도 우선순위를 유지할 수 있지만, 삽입 시 정렬 유지가 O(n). Heap은 삽입/삭제 모두 O(log n)으로 균형 잡힌 성능을 가진다.

## ArrayDeque vs LinkedList

| 항목          | ArrayDeque           | LinkedList         |
|-------------|----------------------|--------------------|
| 구조          | 동적 배열 기반             | 이중 연결 리스트          |
| 삽입/삭제 (양 끝) | O(1) (암시적 포인터 이동)    | O(1) (명시적 포인터 수정 ) |
| 메모리 효율      | 캐시 친화적, 데이터가 메모리상 인접 | 포인터 객체로 분산됨        |
| GC 부담       | 낮음 (순차 배열)           | 높음 (객체 다수 생성)      |
| 성능 최적화 여부   | JDK 내부에서 최적화 많이 됨    | 상대적으로 덜 됨          |

## Queue 인터페이스
| 기능 | 예외 발생     | 특수 값 반환  | 설명                        |
|----|-----------|----------|---------------------------|
| 삽입 | add(e)    | offer(e) | offer는 실패시 `false` 반환     |
| 삭제 | remove()  | poll()   | poll는 큐가 비어있으면 `null`을 반환 |
| 확인 | element() | peek()   | peek은 큐가 비어있으면 `null`을 반환 |

## 관련 문서

- [[자료구조]]
- [[스택]]
- [[Array와 LinkedList]]
