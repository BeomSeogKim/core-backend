---
tags: [computer-science, os, thread]
status: completed
created: 2026-03-04
---

# 스레드 모델: Platform Thread, Context Switching, Virtual Thread

## 핵심 개념

JVM의 **Platform Thread**는 OS Thread와 1:1 매핑되는 무거운 스레드다. 대량 동시 요청 처리 시 **Context Switching** 오버헤드와 메모리 한계가 발생한다. **Virtual Thread**(Project Loom, JDK 21)는 JVM이 관리하는 경량 스레드로, Platform Thread 위에서 N:M 매핑으로 실행되어 이 문제를 해결한다.

> [!note] 스레드 모델 진화
> Platform Thread(1:1) -> Virtual Thread(N:M)로의 진화는, 동시 요청 수가 수천~수만으로 증가하면서 OS Thread의 메모리(~1MB)와 Context Switching 비용이 병목이 된 것이 원인이다.

## 동작 원리

### 사용자 수준 스레드 vs 커널 수준 스레드

스레드는 **누가 관리하느냐**에 따라 두 가지로 나뉜다.

| | 사용자 수준 스레드 | 커널 수준 스레드 |
|---|---|---|
| **관리 주체** | 사용자 공간 라이브러리(런타임) | OS 커널 |
| **OS 인지 여부** | OS는 스레드 존재를 모름 | OS가 직접 관리 |
| **스케줄링** | 라이브러리가 수행 | 커널이 수행 |
| **컨텍스트 스위칭** | 빠름 (커널 개입 없음) | 느림 (커널 모드 전환) |
| **I/O 블로킹** | 하나가 블로킹되면 **프로세스 전체 블로킹** | 해당 스레드만 블로킹 |

> [!warning] 사용자 수준 스레드의 I/O 블로킹 문제
> OS는 사용자 수준 스레드를 모르고 **프로세스 단위**로만 본다. 스레드 A가 I/O로 블로킹되면 OS는 프로세스 전체를 블로킹 → 같은 프로세스의 스레드 B, C도 전부 멈춘다.

### 스레드 매핑 모델

사용자 수준 스레드(U)와 커널 수준 스레드(K)를 어떻게 매핑하느냐에 따라 3가지 모델이 있다.

```
N:1 모델                 1:1 모델                 M:N 모델
┌───┐ ┌───┐ ┌───┐      ┌───┐ ┌───┐ ┌───┐      ┌───┐ ┌───┐ ┌───┐ ┌───┐
│U1 │ │U2 │ │U3 │      │U1 │ │U2 │ │U3 │      │U1 │ │U2 │ │U3 │ │U4 │
└─┬─┘ └─┬─┘ └─┬─┘      └─┬─┘ └─┬─┘ └─┬─┘      └─┬─┘ └─┬─┘ └─┬─┘ └─┬─┘
  │     │     │           │     │     │           │     │     │     │
  └─────┼─────┘           │     │     │           └──┬──┘     └──┬──┘
        │                 │     │     │              │           │
      ┌─┴─┐            ┌─┴─┐ ┌─┴─┐ ┌─┴─┐        ┌──┴──┐    ┌──┴──┐
      │K1 │            │K1 │ │K2 │ │K3 │        │ K1  │    │ K2  │
      └───┘            └───┘ └───┘ └───┘        └─────┘    └─────┘

빠르지만              진정한 병렬,            유연하고 효율적,
하나 블로킹 시        스레드 생성 비용 큼      구현 복잡
전체 블로킹
```

| 모델 | 구조 | 대표 예시 |
|------|------|-----------|
| **N:1** | 사용자 스레드 N개 → 커널 스레드 1개 | Java Green Thread (과거) |
| **1:1** | 사용자 스레드 1개 → 커널 스레드 1개 | Java Platform Thread (현재) |
| **M:N** | 사용자 스레드 M개 → 커널 스레드 N개 | Java Virtual Thread (JDK 21) |

### Platform Thread (OS Thread)

JVM의 기본 스레드. OS 스레드와 **1:1 매핑**.

```
JVM Thread  <->  OS Thread  <->  CPU Core
   (1)              (1)
```

#### 특성
- OS 커널이 스케줄링 담당
- 스택 메모리: ~1MB (OS 영역, 고정 할당)
- 생성 비용: 높음 (OS syscall 필요)
- 최대 개수: 수천 개 (메모리 한계)

```java
// Platform Thread 생성
Thread thread = new Thread(() -> { ... });
thread.start();  // OS에 스레드 생성 요청 (syscall)
```

### Context Switching 비용

CPU 코어는 한 번에 하나의 스레드만 실행. 여러 스레드를 번갈아 실행하기 위해 **컨텍스트 스위칭** 발생.

#### 전환 과정

```
[Thread-A 실행 중]
  CPU 레지스터: [A의 PC, SP, 범용 레지스터들]

OS 스케줄러가 Thread-B로 전환 결정:
  1. User Mode → Kernel Mode (syscall)
  2. Thread-A의 CPU 레지스터 → TCB-A에 저장
  3. 스케줄러가 Thread-B 선택
  4. TCB-B에서 Thread-B의 레지스터 복원
  5. Kernel Mode → User Mode
  6. Thread-B 실행 재개
```

#### 비용 요소

**1. Kernel Mode Transition**
- User Space <-> Kernel Space 전환 자체에 비용
- ~1~10 us per switch

**2. CPU 캐시 Cold Miss**
```
Thread-A 실행 중:
  L1 Cache: [A의 데이터]  ← 따뜻한 캐시

Thread-B로 스위칭:
  L1 Cache: [B의 데이터로 교체]  ← A 데이터 사라짐

Thread-A 재개:
  L1 Cache miss → L2 → L3 → RAM
  L1: ~1ns vs RAM: ~100ns  (100배 차이)
```

**3. TLB Miss** ([[프로세스,스레드|프로세스]] 간 전환 시)
- **Translation Lookaside Buffer** 무효화
- 가상주소 -> 물리주소 변환 캐시 초기화

> [!warning] 컨텍스트 스위칭 지옥
> 10,000개의 Platform Thread를 사용하면 스택만 10GB를 차지하고, 8코어 CPU에서 1초에 수천 번 Context Switching이 발생한다. 실제 작업 시간보다 스위칭 오버헤드가 더 커질 수 있다.

```
요청 10,000개 / Platform Thread 10,000개:
  - 메모리: 10,000 × 1MB = 10GB (스택만)
  - CPU 8코어: 1초에 수천 번 컨텍스트 스위칭
  - 실제 작업 시간 < 스위칭 오버헤드
  → "컨텍스트 스위칭 지옥"
```

### Preemptive vs Cooperative 스케줄링

#### Preemptive (Platform Thread)
OS가 강제로 스레드를 중단하고 다른 스레드로 전환.

```
Thread-A: ████████|강제중단|████████
Thread-B:         |████████|
                  ↑ OS 타임슬라이스 만료
```

- 개발자가 제어 불가
- 공정한 CPU 분배
- 언제든 중단될 수 있음 -> 공유 자원 보호 필요 (synchronized)

#### Cooperative ([[2-Areas/backend/01-languages/kotlin/코루틴-기초|Kotlin Coroutine]])
코루틴이 **자발적으로** 실행권을 양보 (`suspend` 지점에서).

```
Coroutine-A: ████████|suspend|재개████████
Coroutine-B:         |████████|
                     ↑ awaitSingle() 등 suspend point
```

- 개발자가 suspend 지점 명시
- 스레드 전환 없이 같은 스레드에서 스위칭 가능
- suspend point 없는 CPU 작업은 다른 코루틴 실행 막음 -> `yield()` 필요

### Virtual Thread (Project Loom, JDK 21)

#### 개념

JVM이 관리하는 경량 스레드. Platform Thread(carrier thread) 위에서 N:M 실행.

```
Virtual Thread 1 ─┐
Virtual Thread 2 ──┤ Platform Thread 1 (carrier)
Virtual Thread 3 ─┘

Virtual Thread 4 ─┐
Virtual Thread 5 ──┤ Platform Thread 2 (carrier)
Virtual Thread 6 ─┘
```

#### 동작 방식

```
Virtual Thread가 blocking I/O 호출:
  → JVM이 감지
  → Virtual Thread를 carrier thread에서 언마운트
  → Virtual Thread 상태를 힙에 저장
  → carrier thread는 다른 Virtual Thread 실행

I/O 완료:
  → JVM이 Virtual Thread를 carrier thread에 마운트
  → 실행 재개
```

개발자는 그냥 blocking 코드를 작성하면 JVM이 투명하게 처리.

```java
// 그냥 blocking 코드 작성
try (var executor = Executors.newVirtualThreadPerTaskExecutor()) {
    executor.submit(() -> {
        var response = restTemplate.getForObject(url, String.class);  // blocking OK
        return process(response);
    });
}
```

#### 특성

| | Platform Thread | Virtual Thread |
|---|---|---|
| 관리 주체 | OS | JVM |
| 스케줄링 | OS Preemptive | JVM (carrier thread pool) |
| 메모리 | ~1MB 고정 스택 | ~1KB~ 가변 (힙) |
| 최대 개수 | 수천 | 수백만 |
| blocking I/O | 스레드 점유 | JVM이 언마운트 |
| 프로그래밍 모델 | blocking | blocking (투명하게 처리) |

### Coroutine vs Virtual Thread

```
                  Kotlin Coroutine          Virtual Thread
관리 주체          Kotlin 런타임              JVM
전환 방식          Cooperative (suspend)     JVM이 투명하게 처리
개발자 코드         suspend 명시 필요          기존 blocking 코드 그대로
I/O 라이브러리      비동기 API 필요 (WebClient)  동기 API 사용 가능 (RestTemplate)
상태 저장           Continuation 객체 (힙)     가상 스택 (힙)
Backpressure       지원 (Reactor와 통합)      미지원
```

> [!tip] Virtual Thread vs Coroutine 선택 기준
> 기존 blocking 코드베이스(JDBC, RestTemplate)는 Virtual Thread가 적합하고, Backpressure가 필요하거나 이미 WebFlux/Reactor 기반이면 Coroutine이 적합하다.

**Virtual Thread 선택:**
- 기존 blocking 코드베이스 마이그레이션
- JDBC, 동기 HTTP 클라이언트 사용 중
- 간단한 I/O 위주 서비스
- Spring MVC + `spring.threads.virtual.enabled=true`

**Coroutine 선택:**
- Backpressure 필요 (스트리밍, 이벤트 파이프라인)
- 이미 WebFlux/Reactor 기반 코드베이스
- Kotlin 코드베이스 (언어 수준 통합)
- 복잡한 async 파이프라인 구성

## 코드 예시

### Virtual Thread 주의사항: Pinning

`synchronized` 블록 안에서 blocking I/O 시 carrier thread에 고정(pin)됨.

```java
// Pinning 발생 - 피해야 함
synchronized(lock) {
    jdbcTemplate.query(...);  // blocking 중 synchronized 보유
    // → carrier thread가 언마운트 못하고 고정됨
    // → Virtual Thread의 이점 사라짐
}

// 해결책: ReentrantLock 사용
ReentrantLock lock = new ReentrantLock();
lock.lock();
try {
    jdbcTemplate.query(...);  // OK - JVM이 언마운트 가능
} finally {
    lock.unlock();
}
```

> [!warning] JDK 24 이전 synchronized Pinning
> JDK 24 이전에는 `synchronized` 블록 내 blocking I/O가 carrier thread를 고정(pin)시켜 Virtual Thread의 이점이 사라진다. `ReentrantLock`을 사용하거나 JDK 24 이상으로 업그레이드해야 한다.

## 관련 문서

- [[프로세스,스레드]]
- [[컨텍스트-스위칭]]
- [[CPU-Bound,IO-Bound]]
- [[2-Areas/backend/01-languages/kotlin/코루틴-기초|코루틴-기초]]
- [[2-Areas/backend/01-languages/kotlin/코루틴-Dispatcher|코루틴-Dispatcher]]
